[build]
  command = "npm run build"
  publish = ".next"

[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
  
[[headers]]
  for = "/socket.io/*"
  values =
    "Access-Control-Allow-Origin: *"
